#!/usr/bin/env bash
# Git pre-commit hook dispatcher.
# Individual hooks can be enabled/disabled by commenting out lines below.

set -uo pipefail

root="$(git rev-parse --show-toplevel)"

# Regenerate skills README when skill SKILL.md files are staged
"$root/.githooks/pre-commit-skill-gen" || exit 1
