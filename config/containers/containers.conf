# Base Podman configuration shared via dotfiles.
# Host-specific values are appended by bin/migrate-docker-to-podman.sh.

[engine]
# Additional engine options managed via dotfiles belong here.


# Emit logs on each invocation of the compose command indicating
# that an external compose provider is being executed (e.g., docker-compose).
compose_warning_logs=false
