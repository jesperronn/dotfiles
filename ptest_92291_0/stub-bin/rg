#!/usr/bin/env bash
set -euo pipefail

pattern=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    -n|-i|-N)
      shift
      ;;
    *)
      pattern="$1"
      shift
      break
      ;;
  esac
done

grep -Ein "$pattern"

